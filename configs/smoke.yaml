# Smoke-test config: tiny models for fast end-to-end verification.
paths:
  data_dir:        data/CICIDS2017
  prep_dir:        outputs/preprocessing
  models_dir:      models
  outputs_dir:     outputs/models
  results_dir:     results
  logs_dir:        logs

seed: 42

features:
  iqr_lower:         0.01
  iqr_upper:         0.99
  skew_threshold:    1.0
  corr_threshold:    0.95

gmm:
  n_components:          2
  covariance_type:       full
  threshold_percentile:  10
  n_init:                1
  max_iter:              50
  tune_subsample:        2000

isolation_forest:
  n_estimators:    20
  contamination:   0.15
  max_features:    0.5

ocsvm:
  nu:          0.2
  kernel:      rbf
  gamma:       0.1
  train_cap:   1000

phase2:
  model:         lstm_ae
  train_stride:  1
  eval_stride:   1
  max_windows:   null

lstm_ae:
  window_size:      8
  latent_dim:       4
  dropout_rate:     0.0
  lr:               0.001
  epochs:           3
  batch_size:       64
  patience:         3
  val_split:        0.2
  threshold_pct:    95

hybrid:
  alpha:              0.7
  threshold:          0.5
  meta_train_frac:    0.4
  candidates:         [rf, lr, weighted]
  cv_folds:           3

monitor:
  ph_tau:            40
  ph_lambda:         0.1
  ph_alert_tau:      15
  adwin_delta:       0.0005
  adwin_max_window:  2000
  ks_alpha:          0.001
  ks_window:         300

adapt:
  buffer_capacity:     5000
  min_buffer_to_adapt: 80
  min_attempt_interval: 50
  fpr_tolerance:       0.30
  recall_tolerance:    0.05
  validation_fraction: 0.10

evaluation:
  roc_plot:         false
  cm_plot:          false
  score_dist_plot:  false
  per_attack_csv:   false
  report_txt:       false

detect:
  model:            hybrid
  batch_size:       1024
  output_scores:    true
